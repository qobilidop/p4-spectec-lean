/-!
# P4Spec

Planned for milestone M3 in `docs/design.md`, and empty until then: the
full P4 1.2.5 specification rendered into Lean from `exports/p4.al.json`
(exported at M3A; generation barriers summarized in `docs/design.md`),
split per upstream spec section, plus the target instances under
`P4Spec/Targets/` that mirror upstream's `backend-sim/<target>/`.
-/
