/-!
# P4Spec

The full P4 1.2.5 specification rendered into Lean from
`exports/p4.il.json`, split per upstream spec section, plus the target
instances under `P4Spec/Targets/` that mirror upstream's
`backend-sim/<target>/`. Milestone M3 in `docs/design.md`.
-/
