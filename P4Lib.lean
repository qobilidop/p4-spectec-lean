/-!
# P4Lib

P4 primitives for downstream users, planned for milestone M4 and empty
until then: byte strings and header packing (`P4Lib.Packet`), and lemmas
bridging the specification's integer-modulo bit arithmetic to core
`BitVec` (`P4Lib.BitVec`). Independent of the generated specifications.
-/
