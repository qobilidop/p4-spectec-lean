# Consolidated project documentation review

Independent read-only review, 2026-09-26, by `/root/p4_oracle`.

Reviewed the frozen consolidation changes to `docs/design.md`,
`docs/prior-arts-comparison.md`, `.agents/decisions.md`, `.agents/roadmap.md`,
`.agents/notes/design-review.md`, `.agents/notes/full-p4-reconnaissance.md`
and the two navigation additions in `AGENTS.md`, against current main
`499230b`. README, status, archive operations and implementation changes
are outside this review.

No findings. The selective changes retain the newer checked type-runtime
deviations and decisions rather than replacing current documents with the
older branch versions. They distinguish general one-way certificates from
the completed handwritten field-update correspondence, concrete environment
and representation obligations, finite reference realization and transferred
distinct-name commutation. The source domain remains scalar and does not
claim arbitrary assignment reordering, full Nano certification, complete
targets or a whole-corpus result. Broader M3 remains paused.

The prior-art comparison and advisory note identify their pinned historical
baseline and separately record the subsequently adopted direction and PR #27
completion. Prospective recommendations are not presented as completed work
or authorization to resume. Polished documentation does not link into
`.agents/`; the new navigation entries preserve the audience split.

Focused checks in the pinned Nix shell: `scripts/check-text.sh` exit 0 and
`git diff --check` exit 0. Read the complete new comparison and advisory note,
the tracked diffs, and the concrete Example observation APIs. External source
links were not re-fetched; this is consolidation/statement-adequacy review,
not a fresh prior-art fact audit. No Lean build, full gate or remote CI was
run by this reviewer. New files still require root's final staged size gate.

Only this review report was written; no reviewed source or documentation was
changed.
